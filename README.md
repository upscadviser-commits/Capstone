# Market Intelligence App

A web application for market intelligence, featuring a React frontend and a Flask backend.

## Structure
- `/client`: React (Vite + TypeScript) web app.
- `/server`: Flask (Python 3) backend app.

## Development Workflow
This project uses a strict Git Flow. Please develop feature changes in specific feature branches and request validation before merging to `main`.
